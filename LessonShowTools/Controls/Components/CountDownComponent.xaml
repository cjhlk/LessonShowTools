<controls:ComponentBase x:TypeArguments="componentSettings:CountDownComponentSettings" 
                        x:Class="LessonShowTools.Controls.Components.CountDownComponent"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                        xmlns:local="clr-namespace:LessonShowTools.Controls.Components"
                        xmlns:LessonShowTools="clr-namespace:LessonShowTools"
                        xmlns:converters="clr-namespace:LessonShowTools.Converters"
                        xmlns:converters1="clr-namespace:LessonShowTools.Core.Converters;assembly=LessonShowTools.Core"
                        xmlns:controls="clr-namespace:LessonShowTools.Core.Abstractions.Controls;assembly=LessonShowTools.Core"
                        xmlns:models="clr-namespace:LessonShowTools.Models"
                        xmlns:componentSettings="clr-namespace:LessonShowTools.Models.ComponentSettings"
                        mc:Ignorable="d" 
                        d:DesignHeight="450" d:DesignWidth="800"
                        d:DataContext="{d:DesignInstance LessonShowTools:MainWindow}">
    <controls:ComponentBase.Resources>
        <converters1:ColorToColorPickerBrushConverter x:Key="ColorToColorPickerBrushConverter"/>
    </controls:ComponentBase.Resources>
    <Grid DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:CountDownComponent}}"
          VerticalAlignment="Center" TextElement.FontSize="{DynamicResource MainWindowBodyFontSize}">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="距离" Visibility="{Binding Settings.IsCompactModeEnabled, Converter={StaticResource InverseBoolToVisConverter}}"
                       VerticalAlignment="Center" Margin="0 0 2 0"/>
            <TextBlock Text="{Binding Settings.CountDownName}"
                       Foreground="{Binding Settings.FontColor,Converter={StaticResource ColorToColorPickerBrushConverter}}"
                       FontSize="{Binding Settings.FontSize}"
                       VerticalAlignment="Center" Margin="0 0 2 0"
                       FontWeight="Medium"/>
            <TextBlock Text="还有" 
                       VerticalAlignment="Center"
                       Visibility="{Binding Settings.IsCompactModeEnabled, Converter={StaticResource InverseBoolToVisConverter}}"/>
            <TextBlock Text="{Binding DaysLeft, Mode=OneWay}" 
                       Foreground="{Binding Settings.FontColor,Converter={StaticResource ColorToColorPickerBrushConverter}}"
                       FontSize="{Binding Settings.FontSize}"
                       VerticalAlignment="Center"
                       FontWeight="Medium"
                       Margin="2 0 2 0"/>
            <TextBlock Text="天" 
                       Foreground="{Binding Settings.FontColor,Converter={StaticResource ColorToColorPickerBrushConverter}}"
                       FontSize="{Binding Settings.FontSize}"
                       VerticalAlignment="Center"
                       FontWeight="Medium"/>
        </StackPanel>
    </Grid>
</controls:ComponentBase>
